# AI Mouse Controller — Requirements
# Python >= 3.10 recommended
#
# Core hand tracking & UI
numpy==2.2.6
opencv-python==4.12.0.88
mediapipe==0.10.9
protobuf==3.20.3          # mediapipe 0.10.9 requires protobuf 3.x

# Mouse & keyboard control
PyAutoGUI==0.9.54

# Voice Input
SpeechRecognition==3.14.5
PyAudio==0.2.14            # Windows: if pip fails, use: pipwin install pyaudio
keyboard                   # Global hotkey (bat phim khong can focus cua so webcam)
